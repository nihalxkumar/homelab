apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: infrastructure

resources:
  - traefik/
  - cert-manager/
  - axiom/
  - reloader/
  # monitoring/ decommissioned to free ~800Mi on 6Gi node.
  # Re-enable by adding monitoring/ back to resources.
