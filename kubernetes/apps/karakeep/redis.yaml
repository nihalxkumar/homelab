apiVersion: apps/v1
kind: Deployment
metadata:
  name: karakeep-redis
  namespace: karakeep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: karakeep-redis
  template:
    metadata:
      labels:
        app: karakeep-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              memory: "32Mi"
              cpu: "10m"
            limits:
              memory: "128Mi"
              cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: karakeep-redis
  namespace: karakeep
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: karakeep-redis
