apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: thelounge
  namespace: thelounge
  annotations:
    glance/url: https://irc.nihalxkumar.dev
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`irc.nihalxkumar.dev`)
      kind: Rule
      services:
        - name: thelounge
          port: 9000
  tls:
    secretName: nihalxkumar-dev-tls
