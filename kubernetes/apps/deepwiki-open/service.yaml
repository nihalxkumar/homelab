apiVersion: v1
kind: Service
metadata:
  name: deepwiki
  labels:
    app: deepwiki
spec:
  ports:
  - name: ui
    port: 3000
    targetPort: 3000
    protocol: TCP
  - name: api
    port: 8001
    targetPort: 8001
    protocol: TCP
  selector:
    app: deepwiki
